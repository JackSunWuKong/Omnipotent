# OmniFinder (万象探索) - Windows 客户端打包指南

本项目支持生成绿色免安装单文件版（`OmniFinder.exe`，单个独立文件，双击直接运行）。

---

## 在 Windows 电脑上一键自动打包

把当前项目文件夹拷贝到任何一台 Windows 电脑上：
1. **双击运行 `build_for_windows_onefile.bat`**；
2. 构建完成后，直接在 `dist\OmniFinder.exe` 获得单个绿色独立运行程序，双击直接运行！
3. 也可通过 GitHub Actions 自动云端构建并发布在 Releases 中。
