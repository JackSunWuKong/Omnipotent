name: Build Windows Application (Exe)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller PyQt5 PyQtWebEngine httpx beautifulsoup4 yt-dlp requests

      - name: Build with PyInstaller
        run: |
          pyinstaller --noconfirm --onefile --windowed `
            --name="OmniFinder" `
            --add-data="sniffer_magic.py;." `
            --add-data="downloader.py;." `
            --add-data="sniffer_engine.py;." `
            --add-data="video_extractor.py;." `
            --add-data="vip_parser.py;." `
            --add-data="resource_searcher.py;." `
            --add-data="player_server.py;." `
            --add-data="i18n.py;." `
            --add-data="assets;assets" `
            main_ui.py

      - name: Upload Windows Executable
        uses: actions/upload-artifact@v4
        with:
          name: OmniFinder_Windows版
          path: dist/OmniFinder.exe

      - name: Publish to GitHub Releases
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/main'
        with:
          tag_name: latest
          name: "OmniFinder (万象探索) 最新正式版"
          body: "OmniFinder (万象探索) Windows 独立绿色运行程序，双击直接运行，无需配置 Python 环境。支持中/英/繁/日/韩多语言实时切换。本软件仅供个人网络技术研究与多媒体检索学习使用。"
          files: |
            dist/OmniFinder.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
